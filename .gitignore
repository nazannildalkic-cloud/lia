# Nanni: API-Key nicht ins Repo (Sicherheit). Lokal: nanni-config.example.js → nanni-config.js kopieren, Key eintragen.
nanni-config.js

# Umgebungsvariablen / lokale Konfiguration
.env
.env.local
*.local

# Logs
*.log
gateway_*.log

# OS / Editor
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Optional: Node (falls später genutzt)
node_modules/
